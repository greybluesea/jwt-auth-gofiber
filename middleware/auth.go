package middleware

/*import (
	"github.com/gofiber/fiber/v2"
	"github.com/golang-jwt/jwt"
)

func AuthMiddleware() fiber.Handler {
	return func(c *fiber.Ctx) error {
		token := c.Locals("user").(*jwt.Token)
		// Validate the token and extract user information if needed
		// Optionally, you can set the user information in locals for views
		c.Locals("userID", extractedUserID)
		return c.Next()
	}
} */
